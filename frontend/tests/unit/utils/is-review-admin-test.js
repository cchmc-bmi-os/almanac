// import isReviewAdmin from 'almanac/utils/is-review-admin';
import { module, test } from 'qunit';

module('Unit | Utility | is review admin', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    assert.expect(0);
    // let application = startApp();
    // let session = {
    //   data: {
    //     authenticated: {
    //       user_id: 1
    //     }
    //   }
    // };

    // let store = application.__container__.lookup('service:store');

    // let result = isReviewAdmin(session, store);
    // assert.ok(result);

    // destroyApp();
  });
});
