// import { getMetaData } from 'almanac/utils/bugsnag';
import { module, test } from 'qunit';

module('Unit | Utility | bugsnag', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    assert.expect(0);
    // let application = startApp();
    // let result = getMetaData(new Error('testing'), application.__container__);
    // assert.ok(result);
    // destroyApp(application);
  });
});
