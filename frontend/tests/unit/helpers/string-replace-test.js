import { stringReplace } from 'almanac/helpers/string-replace';
import { module, test } from 'qunit';

module('Unit | Helper | string replace', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = stringReplace(42);
    assert.ok(result);
  });
});
